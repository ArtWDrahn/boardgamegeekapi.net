namespace BGGAPI
{
    using BGGAPI.Collection;
    using BGGAPI.Thing;

    public class ThingRequests
    {
        // Filter down to poll results for a specific item.
        //public void PollResults(Item polls, string name)
        //{
        //    foreach (Poll t in polls.Polls)
        //    {
        //        if (t.Title == name)
        //        {
        //            var derp = t.PollList;

        //            // var results = things.Items[0].Polls[0].PollList[0];
           
        //        }
        //    }
        //}

        // Find all items for sale in the marketplace that match a given criteria.
    }
}
